import { PercentInputPipe } from './percent-input.pipe';

describe('PercentInputPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentInputPipe();
    expect(pipe).toBeTruthy();
  });
});

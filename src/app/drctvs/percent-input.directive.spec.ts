import { PercentInputDirective } from './percent-input.directive';

describe('PercentInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentInputDirective();
    expect(directive).toBeTruthy();
  });
});
